<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Aim High Travel and Tours</title>
    <style>
        /* Basic reset */
        * { margin: 0; padding: 0; box-sizing: border-box; }

        /* Body styling */
        body {
            font-family: 'Helvetica Neue', Arial, sans-serif;
            background-color: #f4f7f6;
            color: #333;
            line-height: 1.6;
            display: flex;
            flex-direction: column;
            min-height: 100vh;
            overflow-x: hidden;
        }
        

        /* Container to wrap all content */
        .container {
            width: 100%;
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
            flex-grow: 1; /* Allow container to grow and push sections to the bottom */
        }

        /* Header */
        .header {
            background: #000000; /* Changed from blue to black */
            color: #fff;
            padding: 60px 0;
            text-align: center;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            margin-bottom: 40px;
        }

        .header h1 {
            font-size: 3rem;
            font-weight: 700;
            letter-spacing: 1px;
            margin-bottom: 10px;
        }

        .header p {
            font-size: 1.2rem;
            font-weight: 300;
            margin-bottom: 0;
        }

        /* Section Layout */
        .sections {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
            gap: 20px;
            margin-bottom: 40px;
            margin-top: auto;  /* This will push the sections to the bottom */
        }

        /* Section styling */
        .section {
            background-color: #fff;
            padding: 40px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.08);
            border-radius: 10px;
            text-align: center;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
            margin-top: 20px;
        }

        
        .section h2, .section h3, .section p {
            margin-top: 5px; 
            margin-bottom: 10px; 
        }

        /* Adjust margins for Vision and Mission sections specifically */
        .section h2 {
            font-size: 2.5rem;
            color: #000000; /* Changed from blue to black */
            margin-bottom: 15px;  /* Reduced bottom margin for better alignment */
        }

        .section p {
            font-size: 1.1rem;
            color: #555;
            margin-bottom: 10px;
            line-height: 1.8;
        }
          /* Navbar Styles */
          #navbar {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            background-color: black;
            padding: 10px;
            z-index: 1000;  /* Make sure navbar stays on top */
        }

        .nav-button {
            background-color: black;
            color: white;
            font-size: 1.2rem;
            padding: 10px 20px;
            margin-right: 10px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .nav-button:hover {
            background-color: #444;
        }

        /* Footer */
        .footer {
            background-color: #000000; /* Changed from blue to black */
            color: #fff;
            text-align: center;
            padding: 20px;
            margin-top: 40px;
        }

        .footer p {
            font-size: 1.1rem;
            margin: 0;
        }

        /* Media Queries for responsiveness */
        @media (max-width: 768px) {
            .header h1 {
                font-size: 2.4rem;
            }

            .header p {
                font-size: 1rem;
            }

            .section h2 {
                font-size: 2rem;
            }

            .section p {
                font-size: 1rem;
            }

            .section ul li {
                font-size: 1rem;
            }

            .sections {
                grid-template-columns: 1fr;
            }
        }
